services:
  jellylook:
    build: .
    container_name: jellylook
    env_file: .env
    ports:
      - "${JELLYLOOK_PORT:-3045}:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
